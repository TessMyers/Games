//A password checker! this works fine, DON'T TOUCH IT.

var checkPassword = function() {
  var query = prompt("what's the password?");
  if(query === "password") {
    alert("Awesomesauce, that's right!");
  } else {
    checkPassword();
  }
}